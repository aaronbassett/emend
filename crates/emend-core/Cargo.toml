[package]
name = "emend-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Emend core engine: files, watching, indexing, Markdown parsing, search, AI client. No FFI dependency — fully testable with `cargo test`."

# NOTE: dependency-free skeleton so the workspace builds/tests offline.
# /sdd:implement adds members of the catalog in the root [workspace.dependencies],
# e.g.:  tokio.workspace = true   comrak.workspace = true   nucleo.workspace = true
[dependencies]

[lints]
workspace = true
