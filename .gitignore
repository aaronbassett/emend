# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed — this workspace builds an app binary.

# Rust → Swift build output (regenerated by scripts/build-xcframework.sh)
/xcframework/
/swift/EmendCore/Artifacts/
/swift/EmendCore/Sources/EmendCoreFFI/
*.xcframework

# Swift / Xcode
.DS_Store
DerivedData/
build/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
.swiftpm/
.build/
# The Xcode project is generated reproducibly from app/Emend/project.yml (XcodeGen).
*.xcodeproj/

# Tooling
.lefthook-local.yml
node_modules/

# Secrets — never commit (AI key lives in Keychain, but guard anyway)
*.env
.env*
secrets.*
