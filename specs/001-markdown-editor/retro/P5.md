# Phase 5 Retrospective: US3 — Quick Open

## What Worked Well
<!-- Effective patterns, tools, approaches -->

## What Didn't Work
<!-- Problems, issues, limitations encountered -->

## Workarounds & Solutions
<!-- Bug fixes, non-obvious solutions, clever workarounds -->

## Packages & Dependencies
<!-- Useful packages discovered, packages to avoid, version issues -->

- Quick Open builds on the US2 search index (`emend-core/src/index.rs`, `nucleo-matcher`). The streaming driver (T073) uses the **full `nucleo` 0.5** crate (worker threadpool + `Injector`) already in the catalog, layered over the same `nucleo-matcher` core — keep `emend-core` tokio-free (nucleo uses its own threads). MSRV: verify `nucleo 0.5` compiles under `cargo +1.85` (no declared rust-version; CI enforces).

## Patterns & Code
<!-- Reusable patterns, helpful code snippets, architectural insights -->

- Reuse the existing `SearchSink` foreign-trait sink + `CancellationHandle`/`CancellationToken` scaffolding (`emend-ffi/src/handles.rs`) for `quick_open_query` streaming + supersede-on-keystroke (NFR-002). The watcher's `FsObserver` bridge pattern (Sendable closure hopping to main) applies to the Swift `SearchSink` adapter too.

## For Next Time
<!-- Improvement suggestions, lessons learned, things to try differently -->

- Carried from US1/US2: T078's `QuickOpenTests` is specced as XCUITest, but the XCUITest runner can't bootstrap under CI's `CODE_SIGNING_ALLOWED=NO`. Implement it as a **headless app-hosted test** (drive the Quick Open view-model / search streaming directly), mirroring `EditorPersistenceTests`/`WorkspaceFlowTests`.
- Carried from US2: surface FFI gaps (icon/favorite/pin getters, core-owned app-state persistence) — consider a small follow-up before US6.
